{
  "name": "my-spork",
  "version": "1.0.0",
  "description": "Adv packaging system",
  "main": "index.js",
  "scripts": {
    "build": "node terp-it.js",
    "test": "cd dist && browser-sync start --server --files \"*.js\"",
    "publish": "uglifyjs ./dist/index.js --compress --mangle --output ./dist/index.js",
    "prepublish": "babel src --out-file dist/index.js",
    "pretest": "npm run build"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "async": "^3.1.0",
    "browsersync": "0.0.1-security",
    "fs": "0.0.1-security",
    "path": "^0.12.7",
    "uglify-js": "^3.7.3"
  }
}
